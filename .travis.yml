
language: java
jdk:
  - openjdk17

branches:
  only:
    - main

env:
  global:
    - EMAIL_USERNAME: $EMAIL_USERNAME
    - EMAIL_PASSWORD: $EMAIL_PASSWORD

cache:
  directories:
    - '$HOME/.m2/repository'
    - '$HOME/.gradle'

script: "./gradlew clean build"

notifications:
  email:
    recipients:
      - kimwongi@kakao.com
